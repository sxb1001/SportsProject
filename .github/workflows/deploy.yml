name: deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder deploy
        run: echo "Package Docker image and deploy Lambda/API Gateway infrastructure here."
